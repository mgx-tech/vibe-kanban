# name: Build and publish relay image

# on:
#   workflow_dispatch:
#     inputs:
#       image_tag:
#         description: "Image tag to publish (example: relay-v0.1.32-20260318082626)"
#         required: true
#         default: "latest"
#   push:
#     tags:
#       - "relay-v*"

# jobs:
#   docker:
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       packages: write

#     env:
#       IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/vibekanban-relay

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set tag
#         id: vars
#         shell: bash
#         run: |
#           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
#             echo "tag=${{ github.event.inputs.image_tag }}" >> $GITHUB_OUTPUT
#           else
#             echo "tag=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT
#           fi

#       - name: Set lowercase image name
#         id: image
#         shell: bash
#         run: |
#           OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
#           echo "name=ghcr.io/${OWNER}/vibekanban-relay" >> $GITHUB_OUTPUT

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and push
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: crates/relay-tunnel/Dockerfile
#           push: true
#           tags: |
#             ${{ steps.image.outputs.name }}:${{ steps.vars.outputs.tag }}
#             ${{ steps.image.outputs.name }}:latest
